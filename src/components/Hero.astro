<section class="hero-section">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-7">
                <p class="tech-font mb-2">> Initializing System...</p>
                <h1 class="hero-title mb-4">Ingénieur.<br>Architecte.<br>Créateur.</h1>
                <p class="lead text-gray mb-5" style="max-width: 600px;">
                    Je ne fais pas que coder, je bâtis des écosystèmes complets.
                    De l'IA générative au développement Mobile Offline-First. 
                    <span class="text-white fw-bold">100% Autonome.</span>
                </p>
                <div class="d-flex gap-3 flex-wrap justify-content-center justify-content-lg-start">
                    <a href="#projets" class="btn btn-glow">Voir mes Réalisations</a>
                    <a href="https://github.com/BGBsArt" target="_blank" class="btn btn-outline-light rounded-pill px-4 py-2"><i class="fab fa-github"></i> GitHub</a>
                    <a href="www.linkedin.com/in/gabaki-balode-bgb" target="_blank" class="btn btn-outline-light rounded-pill px-4 py-2"><i class="fab fa-linkedin"></i> LinkedIn</a>
                </div>
            </div>
            
            <div class="col-lg-5 hero-img text-center">
                <div class="profile-container">
                     <img src="/images/projects/profile_bgb.jpg" class="rounded-circle" width="100%" height="100%" />
                    <!-- En attendant, voici l'icone abstraite -->
                    <!-- <i class="fas fa-network-wired fa-5x" style="color: var(--text-gray); opacity: 0.5;"></i> -->
                    
                    <!-- Animation orbitale -->
                    <div class="orbit"></div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>


    /* --- HERO SECTION --- */
    .hero-section {
        min-height: 100vh;
        display: flex;
        align-items: center;
        
        padding-top: 120px; 
        
        background: radial-gradient(circle at top right, rgba(59, 130, 246, 0.1), transparent 40%),
                    radial-gradient(circle at bottom left, rgba(139, 92, 246, 0.1), transparent 40%);
    }


    /* CSS spécifique au Hero */
    .profile-container {
        /* Version MOBILE par défaut (plus petit) */
        width: 250px; 
        height: 250px; 
        
        border-radius: 50%; 
        background: linear-gradient(45deg, #1e293b, #0f172a); 
        border: 2px solid var(--primary-color); 
        margin: auto; 
        display: flex; 
        align-items: center; 
        justify-content: center; 
        position: relative;
        z-index: 10; /* Pour être sûr qu'il passe au dessus */
    }

    .orbit {
        /* Version MOBILE par défaut */
        position: absolute; 
        width: 270px; /* Un peu plus grand que le container */
        height: 270px; 
        
        border: 1px dashed var(--accent-color); 
        border-radius: 50%; 
        animation: spin 20s linear infinite;
        z-index: 5;
    }

    /* Version DESKTOP (Écrans larges) */
    @media (min-width: 992px) {
        .profile-container {
            width: 350px; 
            height: 350px;
        }
        .orbit {
            width: 380px; 
            height: 380px;
        }
    }

    @keyframes spin { 
        100% { transform: rotate(360deg); } 
    }

    /* On force l'affichage sur mobile avec une marge pour l'espacer du texte */
    .hero-img {
        display: block !important; /* Force l'affichage */
        margin-top: 3rem;
    }
</style>